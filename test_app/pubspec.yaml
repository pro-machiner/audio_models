name: test_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  tflite_audio: ^0.3.0
  file_picker: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

assets:
    - assets/decoded_wav_model.tflite
    - assets/decoded_wav_label.txt
    - assets/sample_audio_44k_mono.wav
    - assets/sample_audio_16k_mono.wav
